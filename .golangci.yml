version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - gocyclo
    - misspell
    - unused
  settings:
    gocyclo:
      min-complexity: 30
run:
  timeout: 5m
